# 任务规划算法接口技术文档

## 1. 文档说明

### 1.1 目的

本文档定义任务规划算法的数据输入协议、数据输出协议、字段约束和错误返回格式，用于上层系统与算法模块之间的数据交接。

### 1.2 接口原则

- 调用方以算例目录为单位提交输入。
- 全局配置使用 config.json 描述。
- 任务、固定动作、点位数据使用 CSV 文件传入。
- 距离、时间、能耗使用矩阵 CSV 文件传入。
- 输出由运行结果文件 result.json 和排程文件 schedule.csv 组成。

## 2. 接口总览

### 2.1 输入目录

```
case_001/
  config.json
  task.csv
  package.csv
  point.csv
  distance.csv
  time.csv
  power.csv
```

### 2.2 输出目录

```
case_001/output/
  result.json
  schedule.csv
```

## 3. config.json

### 3.1 文件说明

config.json 描述算法类型、运行参数和全局约束。

### 3.2 示例

```json
{
  "case_id": "instance3",
  "algorithm": {
    "name": "ea",
    "mode": "normal",
    "obj": "maxRevenue",
    "timeLimit": 14400,
    "decimal": 5,
    "random_seed": null
  },
  "max-distance": 10,
  "total-time/day": [300, 410, 380],
  "total-power/day": [3500, 5000, 4000],
  "min-continuous": 3,
  "12-gap": 80,
  "23-gap": 290
}
```

### 3.3 字段定义

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| case_id | string | 是 | 算例唯一标识。 |
| algorithm.name | string | 是 | 算法名称。可选值：ea Gurobi 精确算法、eao SCIP 精确算法、sa 模拟退火、ga 遗传算法、pso 粒子群优化、ai 人工智能算法。 |
| algorithm.mode | string | 是 | 运行模式。可选值：normal 正常规划、revisional 应急规划、back 返回规划。 |
| algorithm.obj | string | 是 | 优化目标。可选值：maxRevenue、minTime、minPower。 |
| algorithm.timeLimit | number/null | 否 | 最大运行时间，单位秒。为空时使用算法默认值。 |
| algorithm.decimal | integer | 否 | 输出数值保留小数位数。 |
| algorithm.random_seed | integer/null | 否 | 随机种子，用于可复现实验。 |
| max-distance | number | 是 | 远端任务最大距离约束。 |
| total-time/day | number[] | 是 | 每日可用时间上限。 |
| total-power/day | number[] | 是 | 每日可用能量上限。 |
| min-continuous | integer | 是 | 连续任务最小完成数量。 |
| 12-gap | number | 是 | 第 1 天至第 2 天跨日约束间隔。 |
| 23-gap | number | 是 | 第 2 天至第 3 天跨日约束间隔。 |

## 4. task.csv

### 4.1 文件说明

task.csv 描述待规划任务集合。

### 4.2 字段

```csv
task_id,task_name,revenue,location,day,time,power,required,continuous,remote,exceptO,tag
```

### 4.3 示例

```csv
task_id,task_name,revenue,location,day,time,power,required,continuous,remote,exceptO,tag
0,执行单点采样1,3,"101,102,103",None,20,133.333333,true,false,false,false,None
1,执行单点采样2,50,"201,202",1,15,100,true,false,true,false,None
```

### 4.4 字段定义

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| task_id | integer | 是 | 任务编号。 |
| task_name | string | 是 | 任务名称。 |
| revenue | number | 是 | 任务收益。 |
| location | string | 否 | 可执行点位，填写 point.csv.point_id。多个点位使用英文逗号分隔；字段含逗号时使用 CSV 引号包裹。None 或空值表示不限制点位。 |
| day | string/number | 否 | 可执行日期。可取 1、2、3、1,2、2,3、1,3。None 或空值表示不限制日期。 |
| time | number | 是 | 任务执行耗时。 |
| power | number | 是 | 任务执行能耗。 |
| required | boolean | 是 | 是否必须完成。 |
| continuous | boolean | 是 | 是否属于连续任务。 |
| remote | boolean | 是 | 是否属于远端任务。若存在远端任务，排程结果至少应完成一个远端任务；远端任务可执行点位需满足 max-distance 约束。 |
| exceptO | boolean | 是 | 是否禁止在原点执行。 |
| tag | string | 否 | 跨日标签。可取 12s、12e、23s、23e、None。 |

### 4.5 task.tag 说明

task.csv 的 tag 用于描述跨日边界任务，只对需要固定在相邻两天开头或末尾的任务填写。

| tag | 含义 |
| --- | --- |
| 12s | 任务可安排在第 1 天或第 2 天的开头。 |
| 12e | 任务可安排在第 1 天或第 2 天的末尾。 |
| 23s | 任务可安排在第 2 天或第 3 天的开头。 |
| 23e | 任务可安排在第 2 天或第 3 天的末尾。 |
| None/空值 | 普通任务，不使用跨日边界约束。 |

带有 s 后缀的任务表示对应相邻天段的开头任务，带有 e 后缀的任务表示对应相邻天段的末尾任务。算法会约束 12s/12e 成对出现在第 1 天或第 2 天，23s/23e 成对出现在第 2 天或第 3 天，并避免同一天同时安排两个开头边界任务或两个末尾边界任务。

## 5. package.csv

### 5.1 文件说明

package.csv 描述固定执行动作集合。

### 5.2 字段

```csv
task_id,task_name,time,power,tag
```

### 5.3 示例

```csv
task_id,task_name,time,power,tag
100,离开科考站,10,40,D1ss
101,适应行走,5,30,D1ss
```

### 5.4 字段定义

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| task_id | integer | 是 | 固定动作编号，从 100 起编号。 |
| task_name | string | 是 | 固定动作名称。 |
| time | number | 是 | 固定动作耗时。 |
| power | number | 是 | 固定动作能耗。 |
| tag | string | 是 | 固定动作标签。可取 D1ss、D1se、D1es、D1ee、D2ss、D2se、D2es、D2ee、D3ss、D3se、D3es、D3ee。 |

### 5.5 package.tag 说明

package.csv 的 tag 用于描述每天固定动作在当天内部的位置。格式为 D + 日期 + 两位阶段码，例如 D1ss。

| tag 段 | 含义 |
| --- | --- |
| D1、D2、D3 | 固定动作属于第 1、2、3 天。 |
| ss | 当天出发前的固定动作。 |
| se | 当天前半段末尾、进入可规划任务前后的固定动作。 |
| es | 当天后半段开头、离开可规划任务前后的固定动作。 |
| ee | 当天末尾的固定动作。 |

因此合法取值为 D1ss、D1se、D1es、D1ee、D2ss、D2se、D2es、D2ee、D3ss、D3se、D3es、D3ee。算法会将同一天四类固定动作的 time/power 从该日 total-time/day 与 total-power/day 中扣除，并在输出 schedule.csv 时按 tag 将固定动作插入每日开头、前半段边界、后半段边界和末尾。

## 6. point.csv

### 6.1 文件说明

point.csv 描述所有可访问点位。

### 6.2 字段

```csv
point_name,point_id,X,Y,comment
```

### 6.3 示例

```csv
point_name,point_id,X,Y,comment
探测起点,0,0,0,探测起点设为坐标原点
单点采样点1-1,101,1584.5459,-1149.0019,单点采样区域1
```

### 6.4 字段定义

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| point_name | string | 是 | 点位名称。 |
| point_id | integer | 是 | 点位编号。0 表示探测起点；10000s+100a+b 表示采样区域 a 下的第 b 个采样点，s=1 为连续采样，s=0 为单点采样。 |
| X | number | 是 | X 坐标，用于缺少距离矩阵时推算距离。 |
| Y | number | 是 | Y 坐标，用于缺少距离矩阵时推算距离。 |
| comment | string | 否 | 点位备注。 |

## 7. 矩阵 CSV

### 7.1 文件说明

矩阵 CSV 由三个文件组成：

| 文件 | 含义 |
| --- | --- |
| distance.csv | 点到点距离矩阵。 |
| time.csv | 点到点移动耗时矩阵。 |
| power.csv | 点到点移动能耗矩阵。 |

### 7.2 矩阵格式

三个矩阵文件使用相同格式。第一行第一列为空，后续列为终点 point_id；第一列为起点 point_id。

```csv
,0,101,102
0,Inf,2.001928,1.933662
101,2.002146,Inf,Inf
102,1.933119,Inf,Inf
```

### 7.3 矩阵约束

- 三个矩阵的行点位集合必须一致。
- 三个矩阵的列点位集合必须一致。
- 行列点位必须存在于 point.csv.point_id。
- 不可达或禁止转移的单元格使用 Inf。
- 自环单元格允许使用 Inf，算法读取后可统一重置为 0。

## 8. 输出 result.json

### 8.1 文件说明

result.json 描述算法运行结果。

### 8.2 成功返回

```json
{
  "case_id": "instance3",
  "status": "success",
  "objective_value": 123.45,
  "schedule_path": "output/schedule.csv",
  "error": null
}
```

### 8.3 失败返回

```json
{
  "case_id": "instance3",
  "status": "infeasible",
  "objective_value": null,
  "schedule_path": null,
  "error": {
    "type": "MIPError",
    "message": "Model is infeasible. Refer to infeasible.ilp"
  }
}
```

### 8.4 字段定义

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| case_id | string | 是 | 算例唯一标识。 |
| status | string | 是 | 运行状态。 |
| objective_value | number/null | 否 | 目标函数值。失败时为空。 |
| schedule_path | string/null | 否 | 排程结果文件路径。失败时为空。 |
| error | object/null | 是 | 错误对象。成功时为 null。 |

### 8.5 error 字段定义

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| type | string | 是 | Python 异常类型名称。 |
| message | string | 是 | Python 异常消息，即 str(error)。 |

### 8.6 status 取值

| status | 说明 |
| --- | --- |
| success | 求解成功。 |
| infeasible | 当前约束下无可行解。 |
| input_error | 输入缺失或格式错误。 |
| max_distance_error | 远端任务距离约束无法满足。 |
| subtour_error | 路径结果存在子回路。 |
| io_error | 输出文件占用或读写失败。 |
| solver_error | 求解器异常。 |

## 9. 输出 schedule.csv

### 9.1 文件说明

schedule.csv 描述算法生成的执行序列。

### 9.2 字段

```csv
day,No,task_id,action,location,time,power,revenue
```

### 9.3 示例

```csv
day,No,task_id,action,location,time,power,revenue
1,1,100,离开科考站,0,10,40,0
1,2,0,执行单点采样1,101,20,133.333333,3
2,1,1,执行单点采样2,201,15,100,50
```

### 9.4 字段定义

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| day | integer | 是 | 所属日期。 |
| No | integer | 是 | 当日步骤编号。 |
| task_id | integer | 是 | 任务或固定动作编号。任务编号对应 task.csv.task_id，固定动作编号对应 package.csv.task_id。 |
| action | string | 是 | 动作或任务名称。 |
| location | integer/string | 否 | 执行点位，优先填写 point.csv.point_id；路径类动作可填写路径描述。 |
| time | number | 否 | 步骤耗时。 |
| power | number | 否 | 步骤能耗。 |
| revenue | number | 否 | 任务收益。固定动作填 0。 |

## 10. 输入校验

算法运行前必须完成以下校验：

- config.json 存在且可解析。
- algorithm.name、algorithm.mode、algorithm.obj 在允许范围内。
- task.csv、package.csv、point.csv、distance.csv、time.csv、power.csv 均存在。
- 所有 CSV 必须包含必填字段，字段名必须与本文档一致。
- total-time/day 与 total-power/day 的数组长度必须一致。
- task.csv.location 中出现的点位必须存在于 point.csv.point_id。
- 三个矩阵的行列点位必须存在于 point.csv.point_id。
- 三个矩阵的行列点位集合必须一致。
- required=true 的任务必须至少存在一个可执行点位和一个可执行日期。

## 11. 数据格式约定

| 类型 | 约定 |
| --- | --- |
| 文件编码 | UTF-8。 |
| CSV 分隔符 | 英文逗号。 |
| CSV 含逗号字段 | 使用双引号包裹。 |
| 布尔值 | 推荐使用 true、false；读取时可兼容 TRUE、FALSE。 |
| 空值 | 使用空字符串或 None。 |
| 不可达矩阵值 | 使用 Inf。 |
| 日期编号 | 从 1 开始。 |

## 12. 错误处理

算法模块应将 Python 异常包装写入 result.json。失败时 status 为对应错误状态，schedule_path 为 null，error.type 填写异常类名，error.message 填写 str(error)。

| Python 异常类型 | status | error 输出规则 |
| --- | --- | --- |
| FileNotFoundError | input_error | error.type="FileNotFoundError"，error.message=str(error)。 |
| ValueError | input_error | error.type="ValueError"，error.message=str(error)。 |
| PermissionError | io_error | error.type="PermissionError"，error.message=str(error)。 |
| MaxDistanceError | max_distance_error | error.type="MaxDistanceError"，error.message=str(error)。 |
| MIPError | infeasible | error.type="MIPError"，error.message=str(error)。 |
| SubtourError | subtour_error | error.type="SubtourError"，error.message=str(error)。 |
| 其他异常 | solver_error | error.type 填写实际异常类型，error.message=str(error)。 |
